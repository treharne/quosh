{
  "name": "quosh-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Reference browser client for Quosh (static site, plain TypeScript).",
  "scripts": {
    "build:wasm": "../tools/build-wasm.sh",
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts",
    "serve": "node build.mjs && node serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}